# 个人博客系统 - 前端

基于 Vue 3 + TypeScript + Vite + Element Plus 的现代化博客前端应用，具有前台展示和后台管理双模块。

## ✨ 技术栈

- **框架**: Vue 3.3.8 (Composition API)
- **语言**: TypeScript 5.2.2
- **构建工具**: Vite 5.0.2
- **UI 框架**: Element Plus 2.4.3
- **图标库**: @element-plus/icons-vue + @iconify/vue
- **状态管理**: Pinia 2.1.7
- **路由**: Vue Router 4.2.5
- **HTTP 客户端**: Axios 1.6.2
- **Markdown**: Marked 10.0.0 + Highlight.js 11.9.0
- **日期处理**: Day.js 1.11.10
- **安全**: DOMPurify 3.3.0 (XSS 防护)
- **密码强度**: zxcvbn 4.4.2
- **数据可视化**: ECharts 6.0.0
- **自动导入**: unplugin-auto-import + unplugin-vue-components

## 📁 项目结构

```
frontend/
├── src/
│   ├── api/                    # API 接口层
│   │   ├── request.ts          # Axios 封装与拦截器
│   │   ├── auth.ts             # 认证相关接口
│   │   ├── article.ts          # 文章 CRUD 接口
│   │   ├── category.ts         # 分类接口
│   │   ├── tag.ts              # 标签接口
│   │   ├── comment.ts          # 评论接口
│   │   ├── media.ts            # 媒体文件接口
│   │   ├── moment.ts           # 说说接口 🆕
│   │   └── index.ts            # API 统一导出
│   │
│   ├── assets/                 # 静态资源
│   │   ├── logo.png            # 网站 Logo
│   │   ├── avatar.jpg          # 默认头像
│   │   └── *.png               # 其他图片资源
│   │
│   ├── components/             # 可复用组件
│   │   ├── article/
│   │   │   └── ArticleCard.vue           # 文章卡片组件
│   │   ├── category/
│   │   │   └── CategoryCard.vue          # 分类卡片组件
│   │   ├── comment/
│   │   │   ├── CommentForm.vue           # 评论表单
│   │   │   └── CommentList.vue           # 评论列表
│   │   ├── common/
│   │   │   ├── BackToTop.vue             # 返回顶部按钮
│   │   │   ├── LikeButton.vue            # 点赞按钮
│   │   │   ├── FavoriteButton.vue        # 收藏按钮
│   │   │   └── ShareButton.vue           # 分享按钮
│   │   ├── editor/
│   │   │   └── MarkdownEditor.vue        # Markdown 编辑器
│   │   ├── media/
│   │   │   └── MediaSelector.vue         # 媒体选择器 🆕
│   │   ├── moment/
│   │   │   ├── MomentCard.vue            # 说说卡片 🆕
│   │   │   └── MomentPublish.vue         # 发布说说 🆕
│   │   ├── layout/
│   │   │   ├── Header.vue                # 页面头部
│   │   │   ├── Footer.vue                # 页面底部
│   │   │   └── CircularMenu.vue          # 圆形菜单
│   │   ├── tag/
│   │   │   └── TagCard.vue               # 标签卡片
│   │   ├── tech/
│   │   │   └── TechStack.vue             # 技术栈展示
│   │   ├── theme/                        # 主题相关组件
│   │   ├── timeline/
│   │   │   ├── Timeline.vue              # 时间线容器
│   │   │   └── TimelineItem.vue          # 时间线项目
│   │   └── PasswordStrength.vue          # 密码强度指示器
│   │
│   ├── composables/            # 组合式 API
│   │   └── usePasswordStrength.ts        # 密码强度验证 Hook
│   │
│   ├── layouts/                # 布局组件
│   │   └── FrontendLayout.vue            # 前台通用布局
│   │
│   ├── router/                 # 路由配置
│   │   ├── index.ts            # 路由主文件 + 守卫
│   │   ├── frontend.ts         # 前台路由配置
│   │   └── admin.ts            # 后台路由配置
│   │
│   ├── stores/                 # Pinia 状态管理
│   │   ├── user.ts             # 用户状态（登录/登出/权限）
│   │   ├── article.ts          # 文章状态
│   │   ├── category.ts         # 分类状态
│   │   ├── tag.ts              # 标签状态
│   │   ├── moment.ts           # 说说状态 🆕
│   │   └── index.ts            # Store 统一导出
│   │
│   ├── types/                  # TypeScript 类型定义
│   │   ├── common.ts           # 通用类型 (ApiResponse, Pagination)
│   │   ├── auth.ts             # 认证类型
│   │   ├── user.ts             # 用户类型
│   │   ├── article.ts          # 文章类型
│   │   ├── category.ts         # 分类类型
│   │   ├── tag.ts              # 标签类型
│   │   ├── comment.ts          # 评论类型
│   │   ├── media.ts            # 媒体类型
│   │   ├── moment.ts           # 说说类型 🆕
│   │   ├── timeline.ts         # 时间线类型
│   │   └── index.ts            # 类型统一导出
│   │
│   ├── views/                  # 页面视图
│   │   ├── Home.vue            # 首页
│   │   ├── About.vue           # 关于页面
│   │   ├── Login.vue           # 登录页面
│   │   ├── ArticleList.vue     # 文章列表页
│   │   ├── ArticleDetail.vue   # 文章详情页
│   │   ├── Categories.vue      # 分类总览页
│   │   ├── CategoryDetail.vue  # 分类详情页
│   │   ├── Tags.vue            # 标签总览页
│   │   ├── TagDetail.vue       # 标签详情页
│   │   ├── Moments.vue         # 说说列表 🆕
│   │   └── admin/              # 后台管理页面
│   │       ├── Layout.vue              # 后台布局
│   │       ├── Dashboard.vue           # 控制台
│   │       ├── ArticleList.vue         # 文章管理列表
│   │       ├── ArticleEdit.vue         # 文章编辑页
│   │       ├── CategoryManagement.vue  # 分类管理
│   │       ├── TagManage.vue           # 标签管理
│   │       ├── CommentManage.vue       # 评论管理
│   │       ├── MediaManage.vue         # 媒体库管理
│   │       └── MomentManage.vue        # 说说管理 🆕
│   │
│   ├── App.vue                 # 根组件
│   ├── main.ts                 # 应用入口
│   ├── auto-imports.d.ts       # 自动生成的导入类型
│   └── components.d.ts         # 自动生成的组件类型
│
├── index.html                  # HTML 入口模板
├── vite.config.ts              # Vite 配置
├── tsconfig.json               # TypeScript 配置
├── tsconfig.node.json          # Node TypeScript 配置
├── package.json                # 项目依赖与脚本
├── package-lock.json           # 依赖锁定文件
├── .env                        # 环境变量
├── .env.example                # 环境变量示例
├── .gitignore                  # Git 忽略配置
├── design-preview.html         # 设计预览文件
└── README.md                   # 项目文档
```

## 🚀 快速开始

### 环境要求

- Node.js >= 16.x
- npm >= 8.x

### 安装依赖

```bash
npm install
```

### 配置环境变量

复制 `.env.example` 为 `.env` 并配置：

```env
VITE_API_BASE_URL=http://localhost:3000/api/v1
```

### 启动开发服务器

```bash
npm run dev
```

访问: http://localhost:5173

### 构建生产版本

```bash
npm run build
```

构建产物位于 `dist/` 目录。

### 预览生产版本

```bash
npm run preview
```

### 代码检查

```bash
npm run lint
```

### 代码格式化

```bash
npm run format
```

## 🎯 核心功能

### 前台展示

#### 页面路由
- **首页** (`/`): 展示最新文章、时间线、技术栈
- **文章列表** (`/article`): 分页展示所有文章
- **文章详情** (`/article/:id`): 文章内容、评论、点赞、收藏
- **分类总览** (`/categories`): 所有分类卡片展示
- **分类详情** (`/category/:id`): 特定分类下的文章
- **标签总览** (`/tags`): 所有标签卡片展示
- **标签详情** (`/tag/:id`): 特定标签下的文章
- **说说广场** (`/moments`): 所有说说列表 🆕
- **关于页面** (`/about`): 个人介绍
- **登录页面** (`/login`): 管理员登录

#### 功能特性
- ✅ 响应式设计，适配移动端
- ✅ Markdown 渲染 + 代码高亮
- ✅ 评论系统（发布、回复、点赞）
- ✅ 文章点赞、收藏、分享
- ✅ 分类和标签筛选
- ✅ 文章搜索功能
- ✅ 时间线展示
- ✅ 技术栈展示
- ✅ 返回顶部按钮
- ✅ 圆形悬浮菜单
- ✅ XSS 防护（DOMPurify）

### 后台管理

#### 管理路由 (需要登录认证)
- **控制台** (`/admin/dashboard`): 数据概览、统计图表
- **文章管理** (`/admin/articles`): 文章列表、创建、编辑、删除
- **文章编辑** (`/admin/articles/create`, `/admin/articles/edit/:id`)
- **分类管理** (`/admin/categories`): 分类 CRUD
- **标签管理** (`/admin/tags`): 标签 CRUD
- **评论管理** (`/admin/comments`): 评论审核、删除
- **媒体管理** (`/admin/media`): 图片/文件上传、管理
- **说说管理** (`/admin/moments`): 说说 CRUD、批量操作 🆕

#### 功能特性
- ✅ 用户登录/登出
- ✅ 登录状态持久化
- ✅ 路由守卫（未登录自动跳转）
- ✅ Token 自动刷新
- ✅ Markdown 编辑器（实时预览）
- ✅ 图片拖拽上传
- ✅ 文章草稿保存
- ✅ 文章状态管理（草稿/已发布）
- ✅ **批量操作**（批量删除、批量发布、批量置顶）
- ✅ **媒体选择器** 🆕 - 统一的媒体文件选择组件
- ✅ 数据统计图表（ECharts）
- ✅ 密码强度验证（zxcvbn）
- ✅ 表单验证

### 说说模块 🆕

- ✅ **发布说说** - 支持文本 + 图片（最多9张）
- ✅ **图片九宫格** - 1/2/3/4/9张图不同布局
- ✅ **可见性控制** - 公开/私密/好友可见
- ✅ **位置信息** - 可选添加位置
- ✅ **置顶功能** - 重要说说置顶
- ✅ **批量删除** - 后台批量管理
- ✅ **权限控制** - 作者/管理员权限
- ✅ **响应式设计** - 移动端适配

## ⚙️ 配置说明

### Vite 配置

#### API 代理

开发环境下，所有 `/api` 请求会自动代理到后端服务器：

```typescript
server: {
  port: 5173,
  host: '0.0.0.0',
  proxy: {
    '/api': {
      target: 'http://localhost:3000',
      changeOrigin: true,
    },
  },
}
```

#### 路径别名

```typescript
resolve: {
  alias: {
    '@': resolve(__dirname, 'src'),
  },
}
```

使用示例: `import { xxx } from '@/api'`

#### 构建优化

自动代码分割：
- `vue-vendor`: Vue 核心库（vue, vue-router, pinia）
- `element-plus`: Element Plus 组件库

### 自动导入

项目配置了 `unplugin-auto-import` 和 `unplugin-vue-components`，无需手动导入：

#### 自动导入的 API

```typescript
// Vue API
ref, computed, watch, onMounted, nextTick...

// Vue Router
useRouter, useRoute

// Pinia
defineStore, storeToRefs
```

#### 自动导入的组件

```vue
<!-- Element Plus 组件 -->
<el-button type="primary">按钮</el-button>
<el-input v-model="value" />
<el-dialog v-model="visible">...</el-dialog>
```

生成的类型文件：
- `src/auto-imports.d.ts`
- `src/components.d.ts`

### TypeScript 配置

- **严格模式**: 启用
- **目标**: ES2020
- **模块解析**: bundler
- **路径映射**: `@/*` → `src/*`

## 📝 开发规范

### 代码风格

- 使用 TypeScript 严格模式
- 使用 Composition API（`<script setup lang="ts">`）
- 组件命名使用 PascalCase
- 文件命名使用 PascalCase（组件）或 camelCase（工具）
- CSS 使用 scoped 样式

### 状态管理

使用 Pinia 进行状态管理：

```typescript
import { useUserStore } from '@/stores'

const userStore = useUserStore()
const { user, isLoggedIn } = storeToRefs(userStore)
```

### API 调用

统一使用封装的 API 模块：

```typescript
import { articleApi } from '@/api'

const { data } = await articleApi.getArticleList({ page: 1, pageSize: 10 })
```

### 类型定义

所有接口响应需定义类型：

```typescript
import type { Article, ApiResponse } from '@/types'

const article = ref<Article | null>(null)
```

## 🔒 安全特性

- **XSS 防护**: 使用 DOMPurify 清理 HTML
- **CSRF 防护**: API 请求自动携带 Token
- **路由守卫**: 未登录自动跳转登录页
- **密码强度**: 使用 zxcvbn 验证密码强度
- **环境变量**: 敏感信息不写入代码

## 🔄 批量操作功能

### 文章管理批量操作

在后台文章管理页面，支持以下批量操作：

#### 可用操作

1. **批量发布** - 将选中的草稿文章一键发布
2. **转为草稿** - 将已发布的文章批量转为草稿
3. **批量置顶** - 将多篇文章同时置顶（仅管理员）
4. **取消置顶** - 批量取消文章置顶（仅管理员）
5. **批量删除** - 一次性删除多篇文章

### 分类管理批量操作 🆕

在后台分类管理页面，新增以下批量操作：

#### 可用操作

1. **批量删除** - 一次性删除多个分类
   - 自动检查分类下是否有文章
   - 包含文章的分类会被跳过，显示详细错误信息
   
2. **批量合并** - 将多个分类合并为一个
   - 选择至少 2 个源分类
   - 选择一个目标分类
   - 所有文章自动迁移到目标分类
   - 源分类被删除

### 标签管理批量操作 🆕

在后台标签管理页面，新增以下批量操作：

#### 可用操作

1. **批量删除** - 一次性删除多个标签
   - 自动解除标签与文章的关联关系
   - 支持部分成功，显示详细失败信息
   
2. **批量合并** - 将多个标签合并为一个
   - 选择至少 2 个源标签
   - 选择一个目标标签
   - 所有文章关联自动迁移到目标标签
   - 自动处理重复关联（同一文章不重复关联相同标签）
   - 源标签被删除

3. **批量更新颜色** - 为多个标签设置相同颜色
   - 选择多个标签
   - 选择一个颜色
   - 一次性更新所有选中标签的颜色
   - 适用于按类别分组标签（如技术标签用蓝色，生活标签用绿色）

### 评论管理批量操作 🆕

在后台评论管理页面，新增以下批量操作：

#### 可用操作

1. **批量删除** - 一次性删除多条评论
   - 支持部分成功，显示详细失败信息
   - 自动删除评论及其所有回复
   
2. **批量审核** - 批量审核通过或取消审核
   - 选择多条评论
   - 一键审核通过或取消审核
   - 返回实际影响的评论数量

#### 使用方法

1. 在列表中勾选需要操作的项目（文章/分类/标签）
2. 点击“批量操作”下拉菜单
3. 选择对应的操作项
4. 对于合并操作，选择目标分类/标签
5. 对于颜色更新，选择新颜色
6. 确认后执行批量操作

#### 技术特点

- ✅ **单次请求**: 所有批量操作通过单个 API 请求完成，性能优异
- ✅ **权限控制**: 自动验证每篇文章/分类/标签的操作权限
- ✅ **详细反馈**: 如果某些操作失败，会显示详细错误信息
- ✅ **事务支持**: 后端使用数据库事务保证数据一致性
- ✅ **操作确认**: 所有危险操作都需要用户确认
- ✅ **智能检测**: 分类删除自动检查文章关联，合并操作自动迁移数据
- ✅ **重复处理**: 标签合并自动处理重复关联，避免数据冗余

## ⚠️ 注意事项

1. **后端服务**: 确保后端 API 服务已启动（默认 http://localhost:3000）
2. **环境变量**: 首次运行前复制 `.env.example` 为 `.env` 并配置
3. **热更新**: 开发环境使用 Vite HMR，修改代码自动刷新
4. **构建部署**: 生产环境使用 `npm run build` 构建后部署 `dist/` 目录
5. **路由模式**: 使用 HTML5 History 模式，需要服务器配置支持
6. **自动生成文件**: `auto-imports.d.ts` 和 `components.d.ts` 已加入 `.gitignore`
7. **批量操作**: 
   - 文章批量置顶功能仅管理员可用
   - 所有分类批量操作仅管理员可用
   - 所有标签批量操作仅管理员可用
   - 所有评论批量操作仅管理员可用
   - 分类/标签合并操作不可逆，请谨慎操作
   - 评论批量删除不可逆，请谨慎操作

## 📸 媒体选择器组件 🆕

### 组件介绍

`MediaSelector` 是一个统一的媒体文件选择组件，提供完整的媒体库浏览、上传、选择功能。

**位置**: `src/components/media/MediaSelector.vue`

### 核心功能

- ✅ **单选/多选模式** - 支持选择单个或多个媒体文件
- ✅ **文件上传** - 内置上传功能，支持拖拽上传
- ✅ **搜索筛选** - 按文件名搜索，按类型筛选
- ✅ **分页加载** - 支持大量文件的分页浏览
- ✅ **实时预览** - 网格视图展示文件缩略图
- ✅ **文件限制** - 可配置文件类型、大小、数量限制
- ✅ **返回格式** - 支持返回 URL 或完整对象

### Props 配置

```typescript
interface MediaSelectorProps {
  // 选择模式
  mode?: 'single' | 'multiple'     // 单选/多选，默认 'single'
  
  // 文件限制
  accept?: string                  // 接受的文件类型，默认 'image/*'
  maxCount?: number                // 多选最大数量，默认 9
  maxSize?: number                 // 单文件最大 MB，默认 5
  
  // 显示控制
  showUpload?: boolean             // 显示上传按钮，默认 true
  showSearch?: boolean             // 显示搜索框，默认 true
  dialogTitle?: string             // 对话框标题
  
  // 返回格式
  returnType?: 'url' | 'object'    // 返回 URL 还是完整对象，默认 'url'
}
```

### 事件

```typescript
// 选择完成
@select: (value: string | string[] | Media | Media[]) => void

// 上传完成
@upload: (media: Media) => void

// 取消选择
@cancel: () => void
```

### 使用示例

#### 示例 1：文章封面选择（单选模式）

```vue
<template>
  <div>
    <!-- 输入框 + 选择按钮 -->
    <el-input v-model="coverImage" placeholder="请选择封面图片">
      <template #append>
        <el-button @click="mediaSelectorRef?.open()" :icon="Picture">
          选择
        </el-button>
      </template>
    </el-input>

    <!-- 媒体选择器 -->
    <MediaSelector
      ref="mediaSelectorRef"
      mode="single"
      return-type="url"
      dialog-title="选择封面图片"
      @select="handleCoverSelect"
    />
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { ElMessage } from 'element-plus'
import { Picture } from '@element-plus/icons-vue'
import MediaSelector from '@/components/media/MediaSelector.vue'

const coverImage = ref('')
const mediaSelectorRef = ref()

const handleCoverSelect = (url: string) => {
  coverImage.value = url
  ElMessage.success('封面图片已选择')
}
</script>
```

#### 示例 2：Markdown 编辑器插入图片（多选模式）

```vue
<template>
  <div>
    <!-- 工具栏按钮 -->
    <el-button @click="mediaSelectorRef?.open()" :icon="Picture">
      插入图片
    </el-button>

    <!-- 编辑器 -->
    <el-input v-model="content" type="textarea" :rows="10" />

    <!-- 媒体选择器 -->
    <MediaSelector
      ref="mediaSelectorRef"
      mode="multiple"
      :max-count="5"
      return-type="url"
      dialog-title="插入图片"
      @select="handleImageInsert"
    />
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { ElMessage } from 'element-plus'
import MediaSelector from '@/components/media/MediaSelector.vue'

const content = ref('')
const mediaSelectorRef = ref()

const handleImageInsert = (urls: string[]) => {
  // 生成 Markdown 图片语法
  const images = urls.map(url => `![图片](${url})`).join('\n')
  content.value += images
  
  ElMessage.success(`已插入 ${urls.length} 张图片`)
}
</script>
```

#### 示例 3：返回完整对象

```vue
<template>
  <MediaSelector
    ref="mediaSelectorRef"
    mode="single"
    return-type="object"
    @select="handleMediaSelect"
  />
</template>

<script setup lang="ts">
import type { Media } from '@/types'

const handleMediaSelect = (media: Media) => {
  console.log('选中的媒体文件:', media)
  console.log('文件名:', media.filename)
  console.log('URL:', media.file_url)
  console.log('尺寸:', media.width, 'x', media.height)
  console.log('大小:', media.file_size)
}
</script>
```

### 已集成页面

媒体选择器已集成到以下页面：

1. **文章编辑页** (`ArticleEdit.vue`)
   - 封面图片选择
   - 模式：单选
   - 自动填充到 `cover_image` 字段

2. **Markdown 编辑器** (`MarkdownEditor.vue`)
   - 内容图片插入
   - 模式：多选（最多 5 张）
   - 自动生成 Markdown 图片语法

### 技术特点

- **响应式设计** - 网格布局自适应不同屏幕尺寸
- **性能优化** - 图片懒加载，搜索防抖（500ms）
- **用户体验** - 动画效果，选中状态反馈，操作提示
- **类型安全** - 完整的 TypeScript 类型定义
- **权限控制** - 自动携带认证 Token
- **错误处理** - 完善的错误提示和异常处理

### 样式特性

- **选中效果** - 蓝色边框 + 选中徽章 + 背景色变化
- **悬停效果** - 轻微放大 + 阴影加深
- **过渡动画** - 选中徽章弹出动画（cubic-bezier）
- **网格布局** - 自动填充，最小宽度 140px
- **深色模式** - 暂不支持（计划中）

### 方法

组件通过 `defineExpose` 暴露以下方法：

```typescript
// 打开选择器
mediaSelectorRef.value?.open()

// 关闭选择器
mediaSelectorRef.value?.close()
```

### 注意事项

1. **后端支持** - 需要后端提供媒体列表和上传接口
2. **Token 认证** - 自动从 localStorage 读取 token
3. **文件类型** - 默认只接受图片，可通过 `accept` 自定义
4. **文件大小** - 默认限制 5MB，可通过 `maxSize` 调整
5. **浏览器兼容** - 使用 `loading="lazy"`，需要现代浏览器支持

## 🌐 浏览器支持

| 浏览器 | 版本 |
|--------|------|
| Chrome | >= 87 |
| Firefox | >= 78 |
| Safari | >= 14 |
| Edge | >= 88 |

## 📦 构建产物

```
dist/
├── assets/
│   ├── vue-vendor.[hash].js     # Vue 核心库
│   ├── element-plus.[hash].js   # Element Plus
│   ├── index.[hash].js          # 业务代码
│   └── index.[hash].css         # 样式文件
└── index.html                   # 入口 HTML
```

## 🤝 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📄 许可证

MIT License

---

**Made with ❤️ using Vue 3 + TypeScript + Vite**
